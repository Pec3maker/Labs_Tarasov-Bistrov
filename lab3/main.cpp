#include <iostream>
#include "Factory.h"
#include "Operators.h"

int main()
{
	/*Point* cords = new Point[4];
	cords[0].x = 0;
	cords[0].y = 0;
	cords[1].x = 1;
	cords[1].y = 3;
	cords[2].x = 4;
	cords[2].y = 3;
	cords[3].x = 5;
	cords[3].y = 0;
	Shape* test = new Trapezoid(cords);

	std::cout << "area " << test->Area() << "\nCenter x: " << test->GetCenter().x << " y: " << test->GetCenter().y << "\n";
	std::cout << test->GetPoint(0).x << ";" << test->GetPoint(0).y << "\n" << test->GetPoint(1).x << ";" << test->GetPoint(1).y << "\n" << test->GetPoint(2).x << ";" << test->GetPoint(2).y << "\n" << test->GetPoint(3).x << ";" << test->GetPoint(3).y << "\n";
	test->Rotate(90);
	std::cout << test->GetPoint(0).x << ";" << test->GetPoint(0).y << "\n" << test->GetPoint(1).x << ";" << test->GetPoint(1).y << "\n" << test->GetPoint(2).x << ";" << test->GetPoint(2).y << "\n" << test->GetPoint(3).x << ";" << test->GetPoint(3).y << "\n";
	std::cout << "area " << test->Area() << "\nCenter x: " << test->GetCenter().x << " y: " << test->GetCenter().y;*/


	return 0;
}